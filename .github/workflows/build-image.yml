name: Build and Push Backend to ACR

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: m324demoacrqba.azurecr.io

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t 324-ex2-docker-gremaud-valentin-emf-mini-notes -f ./Dockerfile ./app

      - name: Login ACR
        uses: azure/docker-login@v2
        with:
          login-server: ${{ env.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Push Docker image to ACR
        run: |
          docker tag 324-ex2-docker-gremaud-valentin-emf-mini-notes:latest ${{ env.ACR_LOGIN_SERVER }}/324-ex2-docker-gremaud-valentin-emf-mini-notes:latest
          docker push ${{ env.ACR_LOGIN_SERVER }}/324-ex2-docker-gremaud-valentin-emf-mini-notes:latest
